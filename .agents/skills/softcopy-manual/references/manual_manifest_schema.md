# Manual Manifest Schema

The manifest must define:

- `manifest_status`
- `doc_type`
- ordered sections
- screenshot catalog with `source`

Allowed screenshot source values:

- `real_running_ui`
- `api_diagram`
- `command_output`

